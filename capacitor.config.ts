import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId:'be.dispatch.tracking', appName:'DispatchTracking', webDir:'dist', bundledWebRuntime:false };
export default config;
